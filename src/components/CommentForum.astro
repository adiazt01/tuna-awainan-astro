---
const {username, date, content,id_map,location } = Astro.props
console.log(date)

function marcar(){
  alert("nadie")
}

---

<div class="comment">
    <div class="comment-author">{username}</div>
    <div class="comment-date">{date}</div>
    <p class="comment-text">{content} </p>
</div>

<style>
  .comment {
    padding: 10px;
    border-bottom: 1px solid var(--border-color);
  }

  .comment:last-child {
    border-bottom: none;
  }

  .comment-author {
    font-weight: 600;
    color: var(--text-color);
  }

  .comment-date {
    font-size: 0.875rem;
    color: var(--secondary-text-color);
    margin-bottom: 5px;
  }

  .comment-text {
    font-size: 1rem;
    color: var(--text-color);
    margin-top: 5px;
  }
  i{
    font-size:20px;
  }
</style>
<script src="https://kit.fontawesome.com/d308b98942.js" crossorigin="anonymous"></script>
<script>
  
</script>
