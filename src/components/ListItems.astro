---
import Item from './Item.astro';

interface Props {
    items:{ label:string}[]
}
const { items } = Astro.props; 
---

<ul>{items.map((item)=>(
    <Item name={item.label}/>
))}</ul>

<style>
    ul{
        margin-top:60px;
    }
</style>